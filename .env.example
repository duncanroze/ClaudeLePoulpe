# Connexion Postgres (Neon) — URL pooler recommandée pour Vercel
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require

# Clé API Anthropic — https://console.anthropic.com/settings/keys
# Nécessaire pour l'oracle (Messages API + outil web_search).
# NB : l'abonnement Claude Max ne couvre PAS l'API, il faut des crédits API.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
